# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# URL pública do site (e-mails de confirmação / reset). Obrigatório no build de produção.
VITE_SITE_URL=https://www.eventfest.com.br

# Google Maps (Embed API + Places Autocomplete no cadastro de eventos)
# Crie em https://console.cloud.google.com/ — habilite Maps Embed API, Places API e Geocoding API
# Restrinja a chave por HTTP referrer (produção + http://localhost:*)
VITE_GOOGLE_MAPS_API_KEY=
