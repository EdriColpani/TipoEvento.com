FROM fixo no código: EventoFest <noreply@eventofest.com.br>
Não existe secret de e-mail.

Do zero — o que precisa:

1) Resend (resend.com)
   - Domínio eventofest.com.br = Verified (DNS ok).
   - API Keys → criar/copiar uma chave (re_...).

2) Supabase → Edge Functions → Secrets (só estes)
   - RESEND_API_KEY = essa chave re_...
   - SUPABASE_URL e SUPABASE_SERVICE_ROLE_KEY o Supabase já injeta nas funções; se faltar SERVICE_ROLE, coloque manualmente (Settings → API → service_role).

3) Deploy
   supabase functions deploy send-free-registration-email --no-verify-jwt

Regra: a RESEND_API_KEY tem que ser da MESMA conta Resend onde eventofest.com.br está verificado.
Se a chave for de outra conta, 403 no from.
