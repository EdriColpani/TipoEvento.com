/** Logo oficial EventFest (PNG Full HD, fundo transparente) — `public/OFICIAL_FULL_HD.png`. */
export const SITE_LOGO_SRC = '/OFICIAL_FULL_HD.png';

export const SITE_LOGO_ALT = 'EventFest — A plataforma que faz tudo acontecer';

/** Logo no header — máximo destaque mantendo proporção ~3:2 (ícone + texto + mascote). */
export const SITE_LOGO_HEADER_CLASS =
    'h-16 w-auto max-w-[14rem] sm:h-[4.5rem] sm:max-w-[17rem] md:h-20 md:max-w-[21rem] lg:h-24 lg:max-w-[25rem] xl:h-28 xl:max-w-[28rem]';

/** Telas de auth (login, senha, convite). */
export const SITE_LOGO_FEATURE_CLASS =
    'h-20 w-auto max-w-[20rem] sm:h-24 sm:max-w-[24rem] md:h-28 md:max-w-[28rem]';

/** Barra interna do header — padding vertical mínimo para a logo ocupar quase toda a altura. */
export const SITE_HEADER_BAR_CLASS = 'px-3 py-2 sm:px-5 sm:py-2.5';

/** Links de navegação no header. */
export const SITE_HEADER_NAV_LINK_CLASS = 'text-base font-medium lg:text-lg';

/** Offset do conteúdo abaixo do header fixo (acompanha a altura da logo). */
export const SITE_HEADER_MAIN_OFFSET_CLASS =
    'pt-[5rem] sm:pt-[5.75rem] md:pt-28 lg:pt-32 xl:pt-36';
