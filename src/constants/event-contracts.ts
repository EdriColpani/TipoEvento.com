/**
 * Valor de `event_contracts.contract_type` usado no fluxo de criação/edição de evento (gestor).
 * Não usar `client_terms` nem `company_registration` nesse fluxo.
 */
export const MANAGER_EVENT_CREATION_CONTRACT_TYPE = 'event_terms';
